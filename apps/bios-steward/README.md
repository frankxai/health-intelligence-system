# BIOS Steward

Premium household operator for the BIOS substrate.

## Run

```bash
cd apps/bios-steward
pnpm install
pnpm dev
# http://localhost:4310
```

## Product loop

Capture dock → ledger (localStorage demo) → protocol start → clinician handoff markdown.

## Design

- Scene brief: `docs/scene-brief.md`
- Chlorophyll Dawn tokens in `src/app/globals.css`
- Signature set-piece: Circadian Ring
- No medical chrome · no purple SaaS fog · no all-caps UI

## Safety

Not medical advice. Ordinary wellness only. See repo root `SAFETY.md`.

## Relation to CLI

Python reference: `PYTHONPATH=bios/src python -m bios_substrate`  
This UI is the T1 steward face of the same primitives.
